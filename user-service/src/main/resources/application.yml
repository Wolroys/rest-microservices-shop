spring:
  application:
    name: user-service
  profiles:
    active: users, micro
  config:
    import: configserver:http://localhost:8888
server:
  port: 0

springdoc:
  swagger-ui:
    config-url: /v3/api-docs/swagger-config
    url: /v3/api-docs
